<section class="create-event">
    <div class="outer-container">
        <fieldset>
            <form {{action "createEvent" on="submit"}}>
                <h1>Heard about an Event? Share it!</h1><br>

                <div class="name-box">
                    <span class="field-description">Name</span>
                    {{input type="text" placeholder="Whats the name of the event?" value=model.title}}
                </div>
                <div class="date-box">
                    <span class="field-description">Date</span>
                    {{input type="date" value=newDate}}
                </div>
                <div class="city-box">
                    <span class="field-description">City</span>
                    {{input type="text" value=model.city}}
                </div>
                <div class="state-box">
                    <span class="field-description">State</span>
                    {{view "select" content=controller.allStatesAsList optionValuePath="content.key" optionLabelPath="content.value" value=model.state}}
                </div>
                <div class="tag-row">
                    <div class="tag-field">
                        <span class="field-description">Tags</span><br>
                        {{events/tag-input placeholder="put your tags here..." tags=allTagsByNames addTag="addTag" removeTag="removeTag"}}
                    </div>
                </div>
                <div class="share-event">
                    <button id="create-button" type="submit">Share!</button>
                    <button {{action "cancel"}}>Cancel</button>
                </div>
            </form>
        </fieldset>
        <div class="event">
            <div class="hover-tile-outer">
                {{{get-state model.city model.state}}}
                <div class="hover-tile-container"
                     style="background: url('danceoff.jpg'); background-position: top; background-size: cover">
                    <div class="hover-tile hover-tile-visible">
                        <span class="title">{{model.title}}</span><br><br>
                        <span class="date">{{format-date newDate}}</span>
                    </div>
                    <div class="hover-tile hover-tile-hidden">
                        <div>
                            {{#each tag in model.tags}}<span class="badge-tag">{{tag.name}}</span> {{/each}}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
